name: Build, run tests, code linting

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

on:
  pull_request:
    branches-ignore:
    - master

jobs:
  code-quality-check:
    name: Check code quality
    uses: CropperBlazor/Cropper.Blazor/.github/workflows/build-test-template.yml@dev
    secrets: inherit

  code-linting:
    name: Code linting
    if: ${{ github.actor != 'dependabot[bot]' }}
    runs-on: ubuntu-latest
    # needs: code-quality-check
    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          # super-linter needs the full git history to get the
          # list of files that changed across commits
          fetch-depth: 0
          
      - name: Super-Linter
        uses: super-linter/super-linter@v8.2.1
        env:
           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

           OUTPUT_FOLDER: Reports
           OUTPUT_DETAILS: detailed
           LOG_LEVEL: WARN

           VALIDATE_ALL_CODEBASE: true
           VALIDATE_MARKDOWN: false
           VALIDATE_JAVASCRIPT_PRETTIER: false

           # Exclude everything not in src/Cropper.Blazor and binaries/minified files
           FILTER_REGEX_EXCLUDE: '^(?!src/Cropper\.Blazor/).*|(^|/)obj/|(^|/)bin/|.*\.min\.css$|.*\.min\.js$'

           JSCPD_CONFIG_FILE: '.jscpd.json'
           HTML_FILE_NAME: '.htmlhintrc'
           CSS_FILE_NAME: '.stylelintrc.json'
